server:
  port: 8972

spring:
  datasource:

    driver-class-name: org.mariadb.jdbc.Driver
    # local에서 사용하는 DB
#    url: jdbc:mariadb://localhost:3306/encore?useUnicode=true
#    username: root
#    password: 123456789

    # cloud에서 사용하는 DB
    url: jdbc:mariadb://localhost:6033/encore?useUnicode=true
    username: work_user
    password: work_passwd

mybatis:
  mapper-locations: mappers/*.xml